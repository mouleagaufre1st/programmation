#include <iostream>
using namespace std;
int main(){
    float a, b;
    if ((a>= 2000) && (a<5000))
    {
        b = (a*0,99);
    }
    if (a>= 5000)
    {
        b = (a*0,98);
    }
    cout<< "le nouveaux prix est de : "<<b; 
}